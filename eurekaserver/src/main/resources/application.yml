server:
  port: 8761
eureka:
  client:
    register-with-eureka: false #是否注册自己 是否在注册中心把自己当成一个客户端注册
    fetch-registry: false #是否获取其他 Eureka Server 服务的数据
    service-url:
      defaultZone: http://localhost:8761/eureka/  #访问注册中心的路径
#  server:
#    enable-self-preservation: false
#    eviction-interval-timer-in-ms: 3000